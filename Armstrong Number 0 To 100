#include <iostream>
using namespace std;
int main()
{
	int rem,cube,sum=0,i,j,num,temp=num;
	cout << "Armstrong between 0 To 100" << "\n";
	for(i=0;i<=100;i++)
	{
		rem=i%10;
		sum = sum + (rem*rem*rem);
		if(sum==i)
		{
			cout << i << "\n";
		}
		i=i/10;
	}
	if(sum==i)
	cout << i;
	return 0;
}
